extends layout

block content
    h1 Fabcar UI - Invoke Transaction
    #data
        if data != undefined && data.length > 0
            p.lead The returned result was: #{data}
        else
            a.btn.btn-lg.btn-primary(href='/actionInvoke') Invoke
script(src='/bootstrap/js/bootstrap.bundle.js')
