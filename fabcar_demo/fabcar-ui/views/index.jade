extends layout

block content
    h1 Fabcar UI
    p.lead Use the menu to execute transactions.
    p Start with Enroll Admin then Register User.
    p They MUST be executed FIRST.
    p Then you can execute Invoke, Query, and API multiple times in any order.
script(src='/bootstrap/js/bootstrap.bundle.js')
