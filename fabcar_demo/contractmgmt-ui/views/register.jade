

block content
    h2 Blockchain POC - Register User
    #data
        if data != undefined && data.length > 0
            p.lead The returned result was: #{data}
        else
            a.btn.btn-lg.btn-primary(href='/actionRegisterUser') Register User

script(src='/bootstrap/js/bootstrap.bundle.js')